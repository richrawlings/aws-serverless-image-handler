{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "CloudFront Distribution"|t('serverless-image-handler'),
    id: 'keyId',
    name: 'keyId',
    suggestEnvVars: true,
    value: fs.keyId,
    errors: fs.getErrors('keyId'),
    class: 'serverless-image-handler-distribution-id',
    instructions: 'Enter the CloudFront Distribution ID that was created whe you set up Serverless Image Handler on AWS'|t('serverless-image-handler')
}) }}
